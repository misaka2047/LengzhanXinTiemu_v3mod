law_peasant_levies = { #征召农兵
	group = lawgroup_army_model
	icon = "gfx/interface/icons/law_icons/peasant_levies.dds"
	
	progressiveness = -50
	
	modifier = {
		unit_offense_mult = -0.2 #进攻
		unit_defense_mult = -0.2 #防御
		unit_morale_loss_mult = 0.1
		state_conscription_rate_add = 0.04
		interest_group_ig_landowners_pol_str_mult = 0.25
		country_military_goods_cost_mult = -0.5
		state_building_barracks_max_level_add = 10
		state_building_conscription_center_max_level_add = 25
	}
	
	on_enact = {
		custom_tooltip = QUALIFICATIONS_OFFICERS_PEASANT_LEVIES_DESC
		if = {
			limit = {
				NOT = {
					has_law = law_type:law_peasant_levies
				}
			}
			custom_tooltip = aristocrats_af_ig_shift_effect_tt
		}
		if = {
			limit = {
				has_law = law_type:law_professional_army
			}
			custom_tooltip = military_pops_af_ig_shift_effect_negative_tt
			custom_tooltip = non_military_pops_af_ig_shift_effect_negative_tt
		}
	}
	
	possible_political_movements = {
		law_professional_army
		law_mass_conscription
		law_national_militia
	}

	pop_support = {
		value = 0
		# Aristocrats like feudalism
		add = {
			desc = "POP_ARISTOCRATS"			
			if = {
				limit = { 
					is_pop_type = aristocrats
				}
				value = 0.33
			}
		}
	}

	# AI will never return to peasant levies
	ai_will_do = {
		always = no
	}

	ai_enact_weight_modifier = { #Petitions
		value = 0
		
		if = {
			limit = { 
				has_journal_entry = je_government_petition
				has_variable = desired_law_var
                scope:law = var:desired_law_var
			}
			add = 750
		}
	}
}

law_professional_army = { #职业军队
	group = lawgroup_army_model
	icon = "gfx/interface/icons/law_icons/professional_army.dds"
	
	progressiveness = 0
	
	unlocking_technologies = { tech_professional_army }

	modifier = {
		country_military_goods_cost_mult = 1
		unit_morale_loss_mult = -0.1
		interest_group_ig_armed_forces_pol_str_mult = 0.25
		state_conscription_rate_add = 0.01
		state_building_barracks_max_level_add = 10
		state_building_conscription_center_max_level_add = 125
		unit_experience_gain_mult = 0.25
	}
	
	possible_political_movements = {
		law_mass_conscription
	}

	on_enact = {
		if = {
			limit = {
				has_law = law_type:law_peasant_levies
			}
			custom_tooltip = aristocrats_af_ig_shift_effect_negative_tt
		}
		if = {
			limit = {
				NOT = {
					has_law = law_type:law_professional_army
				}
			}
			custom_tooltip = non_military_pops_af_ig_shift_effect_tt
			custom_tooltip = military_pops_af_ig_shift_effect_tt
		}
	}

	pop_support = {
		value = 0
		# Officers like professionalism
		add = {
			desc = "POP_OFFICERS"			
			if = {
				limit = { 
					is_pop_type = officers
				}
				value = 0.5
			}
		}
		# soldiers are broadly in favor of this also
		add = {
			desc = "POP_SOLDIERS"		
			if = {
				limit = { 
					is_pop_type = soldiers
				}
				value = 0.25
			}
		}
	}

	ai_enact_weight_modifier = { #Petitions
		value = 0
		
		if = {
			limit = { 
				has_journal_entry = je_government_petition
				has_variable = desired_law_var
                scope:law = var:desired_law_var
			}
			add = 750
		}
	}
}

law_non_militarized_country = { #非军事化国家
	group = lawgroup_army_model
	icon = "gfx/interface/icons/law_icons/非军事化国家.dds"
	
	progressiveness = 0
	
	modifier = {
		country_military_goods_cost_mult = -0.5
		unit_offense_mult = -0.05 #进攻
		unit_defense_mult = -0.05 #防御
		unit_morale_loss_mult = -0.05
		state_conscription_rate_add = 0.05
		interest_group_ig_armed_forces_pol_str_mult = -0.5
		state_building_barracks_max_level_add = 1
		state_building_conscription_center_max_level_add = 100
	}
	
	possible_political_movements = {
		law_professional_army
		law_mass_conscription
	}

	on_enact = {
		if = {
			limit = {
				has_law = law_type:law_peasant_levies
			}
			custom_tooltip = aristocrats_af_ig_shift_effect_negative_tt
		}
		if = {
			limit = {
				has_law = law_type:law_professional_army
			}
			custom_tooltip = military_pops_af_ig_shift_effect_negative_tt
			custom_tooltip = non_military_pops_af_ig_shift_effect_negative_tt
		}
	}

	pop_support = {
		value = 0
		# Farmers like waving a rifle around and telling people to get off their land
		add = {
			desc = "POP_FARMERS"			
			if = {
				limit = { 
					is_pop_type = farmers
				}
				value = 0.25
			}
		}
	}
	
	ai_will_do = {
		exists = ruler
		ruler = {
			has_ideology = ideology:ideology_pacifist
		}
	}

	ai_enact_weight_modifier = { #Petitions
		value = 0
		
		if = {
			limit = { 
				has_journal_entry = je_government_petition
				has_variable = desired_law_var
                scope:law = var:desired_law_var
			}
			add = 750
		}
	}
}

law_national_militia = { #国家民兵
	group = lawgroup_army_model
	icon = "gfx/interface/icons/law_icons/national_militia.dds"
	
	progressiveness = 0
	

	modifier = {
		country_military_goods_cost_mult = -0.25
		unit_offense_mult = -0.05 #进攻
		unit_defense_mult = -0.05 #防御
		unit_morale_loss_mult = -0.05
		state_conscription_rate_add = 0.05
		interest_group_ig_armed_forces_pol_str_mult = -0.25
		state_building_barracks_max_level_add = 5
		state_building_conscription_center_max_level_add = 100
	}
	
	possible_political_movements = {
		law_professional_army
		law_mass_conscription
	}

	on_enact = {
		if = {
			limit = {
				has_law = law_type:law_peasant_levies
			}
			custom_tooltip = aristocrats_af_ig_shift_effect_negative_tt
		}
		if = {
			limit = {
				has_law = law_type:law_professional_army
			}
			custom_tooltip = military_pops_af_ig_shift_effect_negative_tt
			custom_tooltip = non_military_pops_af_ig_shift_effect_negative_tt
		}
	}

	pop_support = {
		value = 0
		# Farmers like waving a rifle around and telling people to get off their land
		add = {
			desc = "POP_FARMERS"			
			if = {
				limit = { 
					is_pop_type = farmers
				}
				value = 0.25
			}
		}
	}
	
	ai_will_do = {
		exists = ruler
		ruler = {
			has_ideology = ideology:ideology_pacifist
		}
	}

	ai_enact_weight_modifier = { #Petitions
		value = 0
		
		if = {
			limit = { 
				has_journal_entry = je_government_petition
				has_variable = desired_law_var
                scope:law = var:desired_law_var
			}
			add = 750
		}
	}
}

law_mass_conscription = {
	group = lawgroup_army_model
	icon = "gfx/interface/icons/law_icons/mass_conscription.dds"
	
	progressiveness = 50

	modifier = {
		unit_morale_loss_mult = 0.05
		country_military_goods_cost_mult = 1.5
		building_training_rate_mult = 1
		state_conscription_rate_add = 0.02
		state_building_barracks_max_level_add = 15
		state_building_conscription_center_max_level_add = 150
	}

	on_enact = {
		if = {
			limit = {
				has_law = law_type:law_peasant_levies
			}
			custom_tooltip = aristocrats_af_ig_shift_effect_negative_tt
		}
		if = {
			limit = {
				has_law = law_type:law_professional_army
			}
			custom_tooltip = military_pops_af_ig_shift_effect_negative_tt
			custom_tooltip = non_military_pops_af_ig_shift_effect_negative_tt
		}
	}
	
	possible_political_movements = {
		law_mass_conscription
	}

	pop_support = {
		value = 0
	}

	ai_enact_weight_modifier = { #Petitions
		value = 0
		
		if = {
			limit = { 
				has_journal_entry = je_government_petition
				has_variable = desired_law_var
                scope:law = var:desired_law_var
			}
			add = 750
		}
	}
}

law_politicized_military = { #政治化军队
	group = lawgroup_army_model
	icon = "gfx/interface/icons/law_icons/政治化军队.dds"
	
	progressiveness = 0

	modifier = {
		country_military_goods_cost_mult = 1
		country_legitimacy_headofstate_add = 20 #军队用物理支持国家元首和其执政党
		state_conscription_rate_add = 0.02
		state_building_barracks_max_level_add = 15
		state_building_conscription_center_max_level_add = 125
	}
	
	possible_political_movements = {
		law_mass_conscription
	}

	pop_support = {
		value = 0
		# Officers like professionalism
		add = {
			desc = "POP_OFFICERS"			
			if = {
				limit = { 
					is_pop_type = officers
				}
				value = 0.5
			}
		}
		# soldiers are broadly in favor of this also
		add = {
			desc = "POP_SOLDIERS"		
			if = {
				limit = { 
					is_pop_type = soldiers
				}
				value = 0.25
			}
		}
	}

	ai_enact_weight_modifier = { #Petitions
		value = 0
		
		if = {
			limit = { 
				has_journal_entry = je_government_petition
				has_variable = desired_law_var
                scope:law = var:desired_law_var
			}
			add = 750
		}
	}
}